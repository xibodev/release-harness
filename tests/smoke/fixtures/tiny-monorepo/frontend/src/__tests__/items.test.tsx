import { describe, it, expect } from "vitest";

describe("items page (deliberately failing)", () => {
  it("DELIBERATE FAIL for unit-integration-test smoke — do not 'fix'", () => {
    expect(1).toBe(2);
  });
});
